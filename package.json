{
  "name": "shridhar-root",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run start --prefix SHRIDHAR-BACKEND",
    "dev:backend": "npm run dev --prefix SHRIDHAR-BACKEND",
    "dev:frontend": "npm run dev --prefix front",
    "install:all": "npm install --prefix SHRIDHAR-BACKEND && npm install --prefix front",
    "build:frontend": "npm run build --prefix front"
  }
}
